# ── JusAds Backend Dependencies ───────────────────────────────────────────────

# API Framework
fastapi
uvicorn
mangum  # Lambda adapter for FastAPI (ASGI → API Gateway)
python-multipart
python-dotenv

# Google AI / Vertex AI
google-genai
google-cloud-speech
google-cloud-aiplatform

# LangGraph / LangChain (agent pipeline orchestration)
langgraph
langchain-core

# Segmentation (SAM 2 for violation region detection)
torch
torchvision
transformers
Pillow

# TTS / Voice (audio remediation)
elevenlabs

# Video Editing (CapCut/JianYing draft generation)
pyJianYingDraft

# Image Editing (Flux Kontext Pro fallback)
requests

# Search / Verification
tavily-python

# AWS (transcription legacy fallback)
boto3

# Data validation
pydantic

# Supabase (structured data persistence)
supabase

# Testing
pytest
hypothesis
pydub
