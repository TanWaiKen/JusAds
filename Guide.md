Process flow
1. When generated, the code will use the previous prompt like

2s to 4 s the woman show armpit, then in thisbcase the 2s to4s video frame will be remove from the video,
Then next is consider does the text compliance or not what I mean is the scripts...... Sometime the video might dont have script then that one can ignore. U need to consider the localization not only see offend or not, but same time, if it not fit the local preferences then u need to find a way to changes

For no scripts case, the only edit video, if scripts got issues then change entire scripts sound.. What I means is to make sure the sound is the same across the video, we can use one sound or dub the original sound. Replace the 2-4s sound with new scripts if using dub, if using sound generation then just change entire video sound

Consider if got script script should always passs along to the video generation models, so that the lips syncing is working,but always define which seconds is what scripts.... 

Usually is bellow ratio
video 5 seconds
sound effect 5 seconds
scripts 4 seconds



Then download a open source video editng frontend, to show the entire correct process of how it is emege of how the deiting is happening......


But remember do not add the fontendf first, each big featurs added, must made it as a stand alone github branches